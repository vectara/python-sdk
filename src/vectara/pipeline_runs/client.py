# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.list_pipeline_runs_response import ListPipelineRunsResponse
from ..types.pipeline_key import PipelineKey
from ..types.pipeline_run import PipelineRun
from ..types.pipeline_run_id import PipelineRunId
from ..types.pipeline_run_status import PipelineRunStatus
from .raw_client import AsyncRawPipelineRunsClient, RawPipelineRunsClient


class PipelineRunsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPipelineRunsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPipelineRunsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPipelineRunsClient
        """
        return self._raw_client

    def list(
        self,
        pipeline_key: PipelineKey,
        *,
        status: typing.Optional[PipelineRunStatus] = None,
        after: typing.Optional[dt.datetime] = None,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[PipelineRun, ListPipelineRunsResponse]:
        """
        List execution runs for a pipeline, with optional filtering by status.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline.

        status : typing.Optional[PipelineRunStatus]
            Filter runs by status.

        after : typing.Optional[dt.datetime]
            Only return runs created after this timestamp.

        limit : typing.Optional[int]
            The maximum number of runs to return.

        page_key : typing.Optional[str]
            Used to retrieve the next page of runs after the limit has been reached.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[PipelineRun, ListPipelineRunsResponse]
            List of pipeline runs.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        response = client.pipeline_runs.list(
            pipeline_key="sharepoint-legal-ingest",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            pipeline_key, status=status, after=after, limit=limit, page_key=page_key, request_options=request_options
        )

    def get(
        self,
        pipeline_key: PipelineKey,
        run_id: PipelineRunId,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PipelineRun:
        """
        Retrieve details of a specific pipeline run including record counts, session keys, and status.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline.

        run_id : PipelineRunId
            The unique identifier of the run.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PipelineRun
            The requested pipeline run.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pipeline_runs.get(
            pipeline_key="sharepoint-legal-ingest",
            run_id="run_pip_abc_manual_550e8400",
        )
        """
        _response = self._raw_client.get(pipeline_key, run_id, request_options=request_options)
        return _response.data


class AsyncPipelineRunsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPipelineRunsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPipelineRunsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPipelineRunsClient
        """
        return self._raw_client

    async def list(
        self,
        pipeline_key: PipelineKey,
        *,
        status: typing.Optional[PipelineRunStatus] = None,
        after: typing.Optional[dt.datetime] = None,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[PipelineRun, ListPipelineRunsResponse]:
        """
        List execution runs for a pipeline, with optional filtering by status.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline.

        status : typing.Optional[PipelineRunStatus]
            Filter runs by status.

        after : typing.Optional[dt.datetime]
            Only return runs created after this timestamp.

        limit : typing.Optional[int]
            The maximum number of runs to return.

        page_key : typing.Optional[str]
            Used to retrieve the next page of runs after the limit has been reached.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[PipelineRun, ListPipelineRunsResponse]
            List of pipeline runs.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            response = await client.pipeline_runs.list(
                pipeline_key="sharepoint-legal-ingest",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            pipeline_key, status=status, after=after, limit=limit, page_key=page_key, request_options=request_options
        )

    async def get(
        self,
        pipeline_key: PipelineKey,
        run_id: PipelineRunId,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PipelineRun:
        """
        Retrieve details of a specific pipeline run including record counts, session keys, and status.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline.

        run_id : PipelineRunId
            The unique identifier of the run.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PipelineRun
            The requested pipeline run.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pipeline_runs.get(
                pipeline_key="sharepoint-legal-ingest",
                run_id="run_pip_abc_manual_550e8400",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(pipeline_key, run_id, request_options=request_options)
        return _response.data
