# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.agent_key import AgentKey
from ..types.agent_session_key import AgentSessionKey
from ..types.list_session_artifacts_response import ListSessionArtifactsResponse
from ..types.session_artifact import SessionArtifact
from .raw_client import AsyncRawAgentArtifactsClient, RawAgentArtifactsClient
from .types.list_agent_artifacts_request_order_by import ListAgentArtifactsRequestOrderBy
from .types.list_agent_artifacts_request_sort_by import ListAgentArtifactsRequestSortBy


class AgentArtifactsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAgentArtifactsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAgentArtifactsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAgentArtifactsClient
        """
        return self._raw_client

    def list(
        self,
        agent_key: AgentKey,
        session_key: AgentSessionKey,
        *,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        sort_by: typing.Optional[ListAgentArtifactsRequestSortBy] = None,
        order_by: typing.Optional[ListAgentArtifactsRequestOrderBy] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[SessionArtifact, ListSessionArtifactsResponse]:
        """
        List all artifacts stored in a specific agent session, with cursor-based pagination. Artifacts are files either uploaded by the user, or generated within a session. This endpoint shows you what files exist in a session, but does not include the file content.

        Parameters
        ----------
        agent_key : AgentKey
            The unique identifier of the agent.

        session_key : AgentSessionKey
            The unique key of the session to list the associated artifacts.

        limit : typing.Optional[int]
            The maximum number of artifacts to return in the list.

        page_key : typing.Optional[str]
            Used to retrieve the next page of artifacts after the limit has been reached.

        sort_by : typing.Optional[ListAgentArtifactsRequestSortBy]
            The field to sort results by.

        order_by : typing.Optional[ListAgentArtifactsRequestOrderBy]
            The ordering direction of the results.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[SessionArtifact, ListSessionArtifactsResponse]
            List of artifacts in the session.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        response = client.agent_artifacts.list(
            agent_key="customer_support",
            session_key="customer_support_chat",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            agent_key,
            session_key,
            limit=limit,
            page_key=page_key,
            sort_by=sort_by,
            order_by=order_by,
            request_options=request_options,
        )

    def get(
        self,
        agent_key: AgentKey,
        session_key: AgentSessionKey,
        artifact_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SessionArtifact:
        """
        Retrieve a specific artifact by its unique `artifact_id`, including metadata and base64-encoded file content.

        Parameters
        ----------
        agent_key : AgentKey
            The unique identifier of the agent.

        session_key : AgentSessionKey
            The unique key of the session.

        artifact_id : str
            The unique identifier of the artifact to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SessionArtifact
            The requested artifact with metadata and base64-encoded content.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.agent_artifacts.get(
            agent_key="customer_support",
            session_key="customer_support_chat",
            artifact_id="art_report_pdf_a3f2",
        )
        """
        _response = self._raw_client.get(agent_key, session_key, artifact_id, request_options=request_options)
        return _response.data


class AsyncAgentArtifactsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAgentArtifactsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAgentArtifactsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAgentArtifactsClient
        """
        return self._raw_client

    async def list(
        self,
        agent_key: AgentKey,
        session_key: AgentSessionKey,
        *,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        sort_by: typing.Optional[ListAgentArtifactsRequestSortBy] = None,
        order_by: typing.Optional[ListAgentArtifactsRequestOrderBy] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[SessionArtifact, ListSessionArtifactsResponse]:
        """
        List all artifacts stored in a specific agent session, with cursor-based pagination. Artifacts are files either uploaded by the user, or generated within a session. This endpoint shows you what files exist in a session, but does not include the file content.

        Parameters
        ----------
        agent_key : AgentKey
            The unique identifier of the agent.

        session_key : AgentSessionKey
            The unique key of the session to list the associated artifacts.

        limit : typing.Optional[int]
            The maximum number of artifacts to return in the list.

        page_key : typing.Optional[str]
            Used to retrieve the next page of artifacts after the limit has been reached.

        sort_by : typing.Optional[ListAgentArtifactsRequestSortBy]
            The field to sort results by.

        order_by : typing.Optional[ListAgentArtifactsRequestOrderBy]
            The ordering direction of the results.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[SessionArtifact, ListSessionArtifactsResponse]
            List of artifacts in the session.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            response = await client.agent_artifacts.list(
                agent_key="customer_support",
                session_key="customer_support_chat",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            agent_key,
            session_key,
            limit=limit,
            page_key=page_key,
            sort_by=sort_by,
            order_by=order_by,
            request_options=request_options,
        )

    async def get(
        self,
        agent_key: AgentKey,
        session_key: AgentSessionKey,
        artifact_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SessionArtifact:
        """
        Retrieve a specific artifact by its unique `artifact_id`, including metadata and base64-encoded file content.

        Parameters
        ----------
        agent_key : AgentKey
            The unique identifier of the agent.

        session_key : AgentSessionKey
            The unique key of the session.

        artifact_id : str
            The unique identifier of the artifact to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SessionArtifact
            The requested artifact with metadata and base64-encoded content.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.agent_artifacts.get(
                agent_key="customer_support",
                session_key="customer_support_chat",
                artifact_id="art_report_pdf_a3f2",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(agent_key, session_key, artifact_id, request_options=request_options)
        return _response.data
