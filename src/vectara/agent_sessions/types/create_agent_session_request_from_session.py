# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ...core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class CreateAgentSessionRequestFromSession(UniversalBaseModel):
    """
    Create a new session by forking an existing one. By default, copies all visible events
    and artifacts from the source session without compaction. Optionally specify exactly one of
    include_up_to_event_id or compact_up_to_event_id to control which events are included
    and whether they are compacted. These two fields are mutually exclusive.
    """

    agent_key: typing.Optional[str] = pydantic.Field(default=None)
    """
    Agent key of the source session (defaults to current agent).
    """

    session_key: str = pydantic.Field()
    """
    Session key of the source session.
    """

    include_up_to_event_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Only include events up to and including this event ID from the source session,
    copying them verbatim. Events after this ID are excluded. If null (the default),
    all visible events are included. Mutually exclusive with compact_up_to_event_id.
    """

    compact_up_to_event_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Compact all source events up to this event ID into a summary instead of
    copying them verbatim. If null (the default), events are copied verbatim.
    Mutually exclusive with include_up_to_event_id.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
