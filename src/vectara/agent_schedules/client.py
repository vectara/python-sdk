# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.agent_input import AgentInput
from ..types.agent_key import AgentKey
from ..types.agent_schedule import AgentSchedule
from ..types.agent_schedule_execution import AgentScheduleExecution
from ..types.agent_schedule_key import AgentScheduleKey
from ..types.agent_schedule_name import AgentScheduleName
from ..types.list_agent_schedule_executions_response import ListAgentScheduleExecutionsResponse
from ..types.list_agent_schedules_response import ListAgentSchedulesResponse
from ..types.schedule_configuration import ScheduleConfiguration
from .raw_client import AsyncRawAgentSchedulesClient, RawAgentSchedulesClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AgentSchedulesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAgentSchedulesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAgentSchedulesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAgentSchedulesClient
        """
        return self._raw_client

    def list(
        self,
        agent_key: AgentKey,
        *,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[AgentSchedule, ListAgentSchedulesResponse]:
        """
        List all schedules for the specified agent. This endpoint returns high-level information about each schedule including execution status and next scheduled execution time.

        Parameters
        ----------
        agent_key : AgentKey
            The unique identifier of the agent to list schedules for.

        limit : typing.Optional[int]
            The maximum number of schedules to return in the list.

        page_key : typing.Optional[str]
            Used to retrieve the next page of schedules after the limit has been reached.

        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.

        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[AgentSchedule, ListAgentSchedulesResponse]
            List of agent schedules.

        Examples
        --------
        from vectara import Vectara

        client = Vectara()
        response = client.agent_schedules.list(
            agent_key="customer_support",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            agent_key,
            limit=limit,
            page_key=page_key,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            request_options=request_options,
        )

    def create(
        self,
        agent_key: AgentKey,
        *,
        name: AgentScheduleName,
        message: typing.Sequence[AgentInput],
        schedule: ScheduleConfiguration,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        key: typing.Optional[AgentScheduleKey] = OMIT,
        description: typing.Optional[str] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        session_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        max_executions_to_keep: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentSchedule:
        """
        Create a new schedule for automatically executing an agent at specified intervals. Each execution creates a new session with the configured message and metadata.
        
        Schedules enable automated agent workflows such as daily reports, periodic monitoring, or regular data processing. The schedule will create sessions tagged with metadata to identify them as scheduled executions.
        
        ## Example request
        
        ```json
        \\$ curl -X POST https://api.vectara.io/v2/agents/support-agent/schedules \\
        -H "Authorization: Bearer YOUR_API_KEY" \\c
        -H "Content-Type: application/json" \\
        -d '{
          "key": "daily-report",
          "name": "Daily Summary Report",
          "message": [{"type": "text", "content": "Generate a summary of today's activities"}],
          "schedule": {
            "type": "interval",
            "interval": "PT24H"
          },
          "session_metadata": {
            "report_type": "daily"
          }
        }'
        ```
        A successful response includes the unique schedule key, configuration, and creation timestamp.
        
        Parameters
        ----------
        agent_key : AgentKey
            The unique key of the agent to create a schedule for.
        
        name : AgentScheduleName
        
        message : typing.Sequence[AgentInput]
            The input message to send to the agent on each scheduled execution.
        
        schedule : ScheduleConfiguration
        
        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.
        
        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.
        
        key : typing.Optional[AgentScheduleKey]
            Optional unique key for the schedule. If not provided, will be auto-generated.
        
        description : typing.Optional[str]
            Optional detailed description of the schedule's purpose.
        
        enabled : typing.Optional[bool]
            Whether the schedule should be active upon creation.
        
        session_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Arbitrary metadata to include in each session created by this schedule.
        
        max_executions_to_keep : typing.Optional[int]
            Maximum number of past execution records to keep. Defaults to 10.
        
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.
        
        Returns
        -------
        AgentSchedule
            The response includes the complete schedule configuration including the unique schedule key, interval, and timestamps.
        
        Examples
        --------
        from vectara import AgentInput_Text, ScheduleConfiguration_Interval, Vectara
        
        client = Vectara()
        client.agent_schedules.create(
            agent_key="customer_support",
            name="Daily Summary Report",
            message=[
                AgentInput_Text(
                    content="I need help with my widget installation",
                )
            ],
            schedule=ScheduleConfiguration_Interval(
                interval="PT24H",
            ),
        )
        """
        _response = self._raw_client.create(
            agent_key,
            name=name,
            message=message,
            schedule=schedule,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            key=key,
            description=description,
            enabled=enabled,
            session_metadata=session_metadata,
            max_executions_to_keep=max_executions_to_keep,
            request_options=request_options,
        )
        return _response.data

    def get(
        self,
        agent_key: AgentKey,
        schedule_key: AgentScheduleKey,
        *,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentSchedule:
        """
        Retrieve the full details of a specific agent schedule using its unique schedule key. The response includes the schedule's configuration, execution history, and timestamps.

        Parameters
        ----------
        agent_key : AgentKey
            The unique key of the agent.

        schedule_key : AgentScheduleKey
            The unique key of the schedule.

        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.

        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentSchedule
            Agent schedule details.

        Examples
        --------
        from vectara import Vectara

        client = Vectara()
        client.agent_schedules.get(
            agent_key="customer_support",
            schedule_key="daily-report",
        )
        """
        _response = self._raw_client.get(
            agent_key,
            schedule_key,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            request_options=request_options,
        )
        return _response.data

    def delete(
        self,
        agent_key: AgentKey,
        schedule_key: AgentScheduleKey,
        *,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Delete an agent schedule. This stops all future executions of the schedule.

        Sessions that were previously created by this schedule are not deleted and remain accessible.

        Parameters
        ----------
        agent_key : AgentKey
            The unique key of the agent.

        schedule_key : AgentScheduleKey
            The unique key of the schedule to delete.

        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.

        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from vectara import Vectara

        client = Vectara()
        client.agent_schedules.delete(
            agent_key="customer_support",
            schedule_key="daily-report",
        )
        """
        _response = self._raw_client.delete(
            agent_key,
            schedule_key,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            request_options=request_options,
        )
        return _response.data

    def update(
        self,
        agent_key: AgentKey,
        schedule_key: AgentScheduleKey,
        *,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        name: typing.Optional[AgentScheduleName] = OMIT,
        description: typing.Optional[str] = OMIT,
        message: typing.Optional[typing.Sequence[AgentInput]] = OMIT,
        schedule: typing.Optional[ScheduleConfiguration] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        session_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        max_executions_to_keep: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentSchedule:
        """
        Update an existing agent schedule. All fields are optional - only provided fields will be updated (PATCH semantics).

        You can pause/resume a schedule by setting the `enabled` field to `false`/`true`. Updating the schedule configuration (interval or cron) will reschedule future executions but will not affect executions currently in progress.

        Parameters
        ----------
        agent_key : AgentKey
            The unique key of the agent.

        schedule_key : AgentScheduleKey
            The unique key of the schedule to update.

        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.

        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.

        name : typing.Optional[AgentScheduleName]

        description : typing.Optional[str]
            Updated description of the schedule's purpose.

        message : typing.Optional[typing.Sequence[AgentInput]]
            Updated input message to send to the agent on each scheduled execution.

        schedule : typing.Optional[ScheduleConfiguration]

        enabled : typing.Optional[bool]
            Updated enabled status for the schedule.

        session_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Updated metadata to include in each session created by this schedule.

        max_executions_to_keep : typing.Optional[int]
            Updated maximum number of past execution records to keep.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentSchedule
            Updated schedule.

        Examples
        --------
        from vectara import Vectara

        client = Vectara()
        client.agent_schedules.update(
            agent_key="customer_support",
            schedule_key="daily-report",
        )
        """
        _response = self._raw_client.update(
            agent_key,
            schedule_key,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            name=name,
            description=description,
            message=message,
            schedule=schedule,
            enabled=enabled,
            session_metadata=session_metadata,
            max_executions_to_keep=max_executions_to_keep,
            request_options=request_options,
        )
        return _response.data

    def list_executions(
        self,
        agent_key: AgentKey,
        schedule_key: AgentScheduleKey,
        *,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[AgentScheduleExecution, ListAgentScheduleExecutionsResponse]:
        """
        List all execution attempts for a schedule, ordered by most recent first.

        Parameters
        ----------
        agent_key : AgentKey

        schedule_key : AgentScheduleKey

        limit : typing.Optional[int]

        page_key : typing.Optional[str]

        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.

        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[AgentScheduleExecution, ListAgentScheduleExecutionsResponse]
            List of schedule execution attempts.

        Examples
        --------
        from vectara import Vectara

        client = Vectara()
        response = client.agent_schedules.list_executions(
            agent_key="customer_support",
            schedule_key="daily-report",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list_executions(
            agent_key,
            schedule_key,
            limit=limit,
            page_key=page_key,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            request_options=request_options,
        )


class AsyncAgentSchedulesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAgentSchedulesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAgentSchedulesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAgentSchedulesClient
        """
        return self._raw_client

    async def list(
        self,
        agent_key: AgentKey,
        *,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[AgentSchedule, ListAgentSchedulesResponse]:
        """
        List all schedules for the specified agent. This endpoint returns high-level information about each schedule including execution status and next scheduled execution time.

        Parameters
        ----------
        agent_key : AgentKey
            The unique identifier of the agent to list schedules for.

        limit : typing.Optional[int]
            The maximum number of schedules to return in the list.

        page_key : typing.Optional[str]
            Used to retrieve the next page of schedules after the limit has been reached.

        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.

        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[AgentSchedule, ListAgentSchedulesResponse]
            List of agent schedules.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara()


        async def main() -> None:
            response = await client.agent_schedules.list(
                agent_key="customer_support",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            agent_key,
            limit=limit,
            page_key=page_key,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            request_options=request_options,
        )

    async def create(
        self,
        agent_key: AgentKey,
        *,
        name: AgentScheduleName,
        message: typing.Sequence[AgentInput],
        schedule: ScheduleConfiguration,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        key: typing.Optional[AgentScheduleKey] = OMIT,
        description: typing.Optional[str] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        session_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        max_executions_to_keep: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentSchedule:
        """
        Create a new schedule for automatically executing an agent at specified intervals. Each execution creates a new session with the configured message and metadata.
        
        Schedules enable automated agent workflows such as daily reports, periodic monitoring, or regular data processing. The schedule will create sessions tagged with metadata to identify them as scheduled executions.
        
        ## Example request
        
        ```json
        \\$ curl -X POST https://api.vectara.io/v2/agents/support-agent/schedules \\
        -H "Authorization: Bearer YOUR_API_KEY" \\c
        -H "Content-Type: application/json" \\
        -d '{
          "key": "daily-report",
          "name": "Daily Summary Report",
          "message": [{"type": "text", "content": "Generate a summary of today's activities"}],
          "schedule": {
            "type": "interval",
            "interval": "PT24H"
          },
          "session_metadata": {
            "report_type": "daily"
          }
        }'
        ```
        A successful response includes the unique schedule key, configuration, and creation timestamp.
        
        Parameters
        ----------
        agent_key : AgentKey
            The unique key of the agent to create a schedule for.
        
        name : AgentScheduleName
        
        message : typing.Sequence[AgentInput]
            The input message to send to the agent on each scheduled execution.
        
        schedule : ScheduleConfiguration
        
        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.
        
        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.
        
        key : typing.Optional[AgentScheduleKey]
            Optional unique key for the schedule. If not provided, will be auto-generated.
        
        description : typing.Optional[str]
            Optional detailed description of the schedule's purpose.
        
        enabled : typing.Optional[bool]
            Whether the schedule should be active upon creation.
        
        session_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Arbitrary metadata to include in each session created by this schedule.
        
        max_executions_to_keep : typing.Optional[int]
            Maximum number of past execution records to keep. Defaults to 10.
        
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.
        
        Returns
        -------
        AgentSchedule
            The response includes the complete schedule configuration including the unique schedule key, interval, and timestamps.
        
        Examples
        --------
        import asyncio
        
        from vectara import (
            AgentInput_Text,
            AsyncVectara,
            ScheduleConfiguration_Interval,
        )
        
        client = AsyncVectara()
        
        
        async def main() -> None:
            await client.agent_schedules.create(
                agent_key="customer_support",
                name="Daily Summary Report",
                message=[
                    AgentInput_Text(
                        content="I need help with my widget installation",
                    )
                ],
                schedule=ScheduleConfiguration_Interval(
                    interval="PT24H",
                ),
            )
        
        
        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            agent_key,
            name=name,
            message=message,
            schedule=schedule,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            key=key,
            description=description,
            enabled=enabled,
            session_metadata=session_metadata,
            max_executions_to_keep=max_executions_to_keep,
            request_options=request_options,
        )
        return _response.data

    async def get(
        self,
        agent_key: AgentKey,
        schedule_key: AgentScheduleKey,
        *,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentSchedule:
        """
        Retrieve the full details of a specific agent schedule using its unique schedule key. The response includes the schedule's configuration, execution history, and timestamps.

        Parameters
        ----------
        agent_key : AgentKey
            The unique key of the agent.

        schedule_key : AgentScheduleKey
            The unique key of the schedule.

        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.

        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentSchedule
            Agent schedule details.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara()


        async def main() -> None:
            await client.agent_schedules.get(
                agent_key="customer_support",
                schedule_key="daily-report",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(
            agent_key,
            schedule_key,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            request_options=request_options,
        )
        return _response.data

    async def delete(
        self,
        agent_key: AgentKey,
        schedule_key: AgentScheduleKey,
        *,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Delete an agent schedule. This stops all future executions of the schedule.

        Sessions that were previously created by this schedule are not deleted and remain accessible.

        Parameters
        ----------
        agent_key : AgentKey
            The unique key of the agent.

        schedule_key : AgentScheduleKey
            The unique key of the schedule to delete.

        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.

        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara()


        async def main() -> None:
            await client.agent_schedules.delete(
                agent_key="customer_support",
                schedule_key="daily-report",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(
            agent_key,
            schedule_key,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            request_options=request_options,
        )
        return _response.data

    async def update(
        self,
        agent_key: AgentKey,
        schedule_key: AgentScheduleKey,
        *,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        name: typing.Optional[AgentScheduleName] = OMIT,
        description: typing.Optional[str] = OMIT,
        message: typing.Optional[typing.Sequence[AgentInput]] = OMIT,
        schedule: typing.Optional[ScheduleConfiguration] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        session_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        max_executions_to_keep: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentSchedule:
        """
        Update an existing agent schedule. All fields are optional - only provided fields will be updated (PATCH semantics).

        You can pause/resume a schedule by setting the `enabled` field to `false`/`true`. Updating the schedule configuration (interval or cron) will reschedule future executions but will not affect executions currently in progress.

        Parameters
        ----------
        agent_key : AgentKey
            The unique key of the agent.

        schedule_key : AgentScheduleKey
            The unique key of the schedule to update.

        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.

        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.

        name : typing.Optional[AgentScheduleName]

        description : typing.Optional[str]
            Updated description of the schedule's purpose.

        message : typing.Optional[typing.Sequence[AgentInput]]
            Updated input message to send to the agent on each scheduled execution.

        schedule : typing.Optional[ScheduleConfiguration]

        enabled : typing.Optional[bool]
            Updated enabled status for the schedule.

        session_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Updated metadata to include in each session created by this schedule.

        max_executions_to_keep : typing.Optional[int]
            Updated maximum number of past execution records to keep.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentSchedule
            Updated schedule.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara()


        async def main() -> None:
            await client.agent_schedules.update(
                agent_key="customer_support",
                schedule_key="daily-report",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            agent_key,
            schedule_key,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            name=name,
            description=description,
            message=message,
            schedule=schedule,
            enabled=enabled,
            session_metadata=session_metadata,
            max_executions_to_keep=max_executions_to_keep,
            request_options=request_options,
        )
        return _response.data

    async def list_executions(
        self,
        agent_key: AgentKey,
        schedule_key: AgentScheduleKey,
        *,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_timeout: typing.Optional[int] = None,
        request_timeout_millis: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[AgentScheduleExecution, ListAgentScheduleExecutionsResponse]:
        """
        List all execution attempts for a schedule, ordered by most recent first.

        Parameters
        ----------
        agent_key : AgentKey

        schedule_key : AgentScheduleKey

        limit : typing.Optional[int]

        page_key : typing.Optional[str]

        request_timeout : typing.Optional[int]
            The API will make a best effort to complete the request in the specified seconds or time out.

        request_timeout_millis : typing.Optional[int]
            The API will make a best effort to complete the request in the specified milliseconds or time out.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[AgentScheduleExecution, ListAgentScheduleExecutionsResponse]
            List of schedule execution attempts.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara()


        async def main() -> None:
            response = await client.agent_schedules.list_executions(
                agent_key="customer_support",
                schedule_key="daily-report",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list_executions(
            agent_key,
            schedule_key,
            limit=limit,
            page_key=page_key,
            request_timeout=request_timeout,
            request_timeout_millis=request_timeout_millis,
            request_options=request_options,
        )
