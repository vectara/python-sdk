# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .agent_keyed_search_corpus_corpus_key import AgentKeyedSearchCorpusCorpusKey
from .agent_keyed_search_corpus_metadata_filter import AgentKeyedSearchCorpusMetadataFilter
from .custom_dimensions import CustomDimensions
from .search_semantics import SearchSemantics


class AgentKeyedSearchCorpus(UniversalBaseModel):
    """
    Agent-specific keyed search corpus that supports eager references for corpus_key and metadata_filter.
    """

    corpus_key: AgentKeyedSearchCorpusCorpusKey = pydantic.Field()
    """
    The corpus to search. Can be either a static string (e.g. "my-corpus") or an EagerReference object (e.g. {"$ref": "session.metadata.corpus"}). When an EagerReference is provided, the $ref path is resolved from the session context at the start of each turn, before LLM processing begins.
    """

    custom_dimensions: typing.Optional[CustomDimensions] = None
    metadata_filter: typing.Optional[AgentKeyedSearchCorpusMetadataFilter] = pydantic.Field(default=None)
    """
    The filter to narrow search results. Can be either a static string (e.g. "doc.department = 'engineering'") or an EagerReference object (e.g. {"$ref": "session.metadata.filters.user"}). When an EagerReference is provided, the $ref path is resolved from the session context at the start of each turn, before LLM processing begins.
    """

    lexical_interpolation: typing.Optional[float] = pydantic.Field(default=None)
    """
    How much to weigh lexical scores compared to the embedding score. 0 means lexical search is not used at all, and 1 means only lexical search is used.
    """

    semantics: typing.Optional[SearchSemantics] = None
    query: typing.Optional[str] = pydantic.Field(default=None)
    """
    Query override for this specific corpus.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
