# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .pipeline_key import PipelineKey
from .pipeline_name import PipelineName
from .pipeline_source import PipelineSource
from .pipeline_status import PipelineStatus
from .pipeline_sync_mode import PipelineSyncMode
from .pipeline_transform import PipelineTransform
from .pipeline_trigger import PipelineTrigger
from .pipeline_watermark import PipelineWatermark


class Pipeline(UniversalBaseModel):
    """
    A pipeline is an automated data flow that sends all data from a source system to an agent
    for the agent to act on per its instructions. A new agent session is created for each source
    record, giving the agent full autonomy to decide what to do with each piece of data — index it
    into a corpus, extract structured information, route it, or discard it.

    This is distinct from an **agent schedule**, which is a recurring single execution of an
    agent with a fixed message. A schedule creates one session per trigger; a pipeline creates
    a session per source record.

    This is also distinct from an **agent connector** (e.g. Slack), which is a bidirectional
    integration for interactive chat with an agent. A pipeline is a one-directional automated
    flow of all data through an agent.
    """

    key: PipelineKey
    name: PipelineName
    description: typing.Optional[str] = pydantic.Field(default=None)
    """
    A description of what this pipeline does.
    """

    source: PipelineSource
    trigger: PipelineTrigger
    transform: PipelineTransform
    sync_mode: PipelineSyncMode
    watermark: typing.Optional[PipelineWatermark] = pydantic.Field(default=None)
    """
    The current incremental sync watermark. Null if the pipeline has never run.
    """

    status: PipelineStatus
    status_message: typing.Optional[str] = pydantic.Field(default=None)
    """
    Human-readable status details, such as an error description.
    """

    enabled: bool = pydantic.Field()
    """
    Whether the pipeline is enabled. Disabled pipelines do not run on their trigger schedule.
    """

    metadata: typing.Optional[typing.Dict[str, str]] = pydantic.Field(default=None)
    """
    Arbitrary key-value metadata for the pipeline.
    """

    created_at: dt.datetime = pydantic.Field()
    """
    When the pipeline was created.
    """

    updated_at: typing.Optional[dt.datetime] = pydantic.Field(default=None)
    """
    When the pipeline was last updated.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
