# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel, update_forward_refs
from ..core.serialization import FieldMetadata


class JsonSchemaSpec(UniversalBaseModel):
    """
    Specification wrapper for a named JSON schema.
    """

    description: typing.Optional[str] = pydantic.Field(default=None)
    """
    A description of what the response format is for, used by the model to determine how to respond in the format.
    """

    name: str = pydantic.Field()
    """
    A unique name identifier for this schema.
    """

    strict: typing.Optional[bool] = pydantic.Field(default=None)
    """
    When true, enforces strict schema adherence. The model will always follow the exact schema structure.
    When strict mode is enabled, the schema must follow these rules:
    - `additionalProperties: false` must be set on all object types
    - All properties must be listed in the `required` array
    - Maximum 100 properties total, with max 5 levels of nesting
    - Unsupported keywords: minLength, maxLength, pattern, minimum, maximum, minItems, maxItems
    - Root schema cannot use `anyOf` type
    """

    schema_: typing_extensions.Annotated[
        "JsonSchemaDefinition",
        FieldMetadata(alias="schema"),
        pydantic.Field(alias="schema", description="The JSON Schema definition that the model output must conform to."),
    ]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


from .json_schema_definition import JsonSchemaDefinition  # noqa: E402, I001

update_forward_refs(JsonSchemaSpec, JsonSchemaDefinition=JsonSchemaDefinition)
