# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .agent_key import AgentKey


class AgentVerification(UniversalBaseModel):
    """
    Verify the worker agent's output using a separate judge agent. The judge agent receives
    a summary of the worker agent's session and must produce a structured output with
    `{ "success": boolean, "reason": string }`. The judge agent must be configured with a
    structured output parser matching this schema.
    """

    agent_key: AgentKey = pydantic.Field()
    """
    The key of the judge agent that evaluates the worker agent's output.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
