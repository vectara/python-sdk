# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2
from .agent_input import AgentInput
from .create_input_request_base import CreateInputRequestBase
from .input_behavior import InputBehavior


class CreateInputMessageRequest(CreateInputRequestBase):
    """
    A user input message with text or other content.
    """

    messages: typing.List[AgentInput] = pydantic.Field()
    """
    List of inputs that make up this event.
    """

    since: typing.Optional[str] = pydantic.Field(default=None)
    """
    Event ID to return all events created after queueing the new input. When provided and the session is currently running, the response will include all events created after this event ID, and the new input will be processed according to the behavior field. Use the special value "start" to receive all events from the beginning of the session, including the input message itself.
    """

    behavior: typing.Optional[InputBehavior] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
