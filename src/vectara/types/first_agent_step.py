# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel, update_forward_refs
from .agent_output_parser import AgentOutputParser
from .agent_step_instruction import AgentStepInstruction
from .agent_step_reminder import AgentStepReminder
from .next_step import NextStep


class FirstAgentStep(UniversalBaseModel):
    """
    The entry point step for an agent, with a unique name.
    See AgentStep for full step documentation.
    """

    name: str = pydantic.Field()
    """
    Unique identifier for this step within the agent. Must not conflict with any key in the steps map.
    """

    type: typing.Optional[str] = pydantic.Field(default=None)
    """
    Deprecated. This field is ignored and exists only for backward compatibility.
    """

    instructions: typing.List[AgentStepInstruction] = pydantic.Field()
    """
    List of instructions that guide the agent's behavior in this step.
    """

    output_parser: AgentOutputParser
    reminders: typing.Optional[typing.List[AgentStepReminder]] = pydantic.Field(default=None)
    """
    Reminders injected into conversation messages when specific events occur.
    """

    next_steps: typing.Optional[typing.List[NextStep]] = pydantic.Field(default=None)
    """
    Conditional transitions to other steps.
    Evaluated in order; first matching condition is selected.
    A next_step without a condition acts as a catch-all/default.
    If no condition matches, the agent remains on the current step and the agent ends output.
    """

    allowed_tools: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    List of tool configuration names that this step is allowed to use.
    If not specified (null), all tools defined in the agent's tool_configurations are allowed.
    If set to an empty array, no tools are allowed and the agent can only respond with text.
    """

    allowed_skills: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    List of skill names that this step is allowed to use.
    If not specified (null), all skills defined in the agent's skills map are allowed.
    If set to an empty array, no skills are available and the invoke_skill tool is not shown.
    """

    reentry_step: typing.Optional[str] = pydantic.Field(default=None)
    """
    Step name to resume at when the session receives the next user message.
    If not specified, the session re-enters at this step (i.e., stays here).
    Set to the agent's first_step_name to always restart from the beginning.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


update_forward_refs(FirstAgentStep)
