# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .filter_attribute_numeric_stats import FilterAttributeNumericStats
from .filter_attribute_stat_type import FilterAttributeStatType
from .filter_attribute_value import FilterAttributeValue


class FilterAttributeStat(UniversalBaseModel):
    """
    Aggregation result for a single filter attribute, showing the distribution of values and optional statistics for numeric types.
    """

    name: str = pydantic.Field()
    """
    The qualified field name in the format 'level.fieldname' (e.g., 'doc.category' for document-level or 'part.status' for part-level metadata). This name can be used in metadata filter expressions.
    """

    type: FilterAttributeStatType = pydantic.Field()
    """
    The data type of this filter attribute, which determines the aggregation behavior. Numeric types (integer, real_number, and their list variants) include statistical aggregations, while text and boolean types provide value distributions.
    """

    description: typing.Optional[str] = pydantic.Field(default=None)
    """
    Human-readable description of what this field represents, as defined in the corpus filter attribute schema. May be empty if no description was provided during corpus creation.
    """

    values: typing.List[FilterAttributeValue] = pydantic.Field()
    """
    Array of the most frequently occurring distinct values for this field, ordered by occurrence count (descending). Limited by the max_values query parameter. For list-type attributes, each array element is counted independently.
    """

    stats: typing.Optional[FilterAttributeNumericStats] = pydantic.Field(default=None)
    """
    Statistical aggregations for numeric types (integer, real_number, and their list variants). Not present for text or boolean types. Provides min, max, average, and sum across all values.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
