# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class CronScheduleConfiguration(UniversalBaseModel):
    """
    Configuration for cron-based schedule execution.
    """

    cron_expression: str = pydantic.Field()
    """
    Cron expression specifying when the schedule should execute.
    
    Uses standard 5-field cron format:
    ┌───────────── minute (0-59)
    │ ┌───────────── hour (0-23)
    │ │ ┌───────────── day of month (1-31)
    │ │ │ ┌───────────── month (1-12)
    │ │ │ │ ┌───────────── day of week (0-6, Sunday=0)
    │ │ │ │ │
    * * * * *
    
    Special characters:
    - * : Any value
    - , : List (e.g., "1,15" for 1st and 15th)
    - - : Range (e.g., "1-5" for Monday-Friday)
    - / : Step (e.g., "* /15" for every 15 units)
    
    Common examples:
    - "0 9 * * *": Every day at 9:00 AM
    - "0 9 * * 1-5": Weekdays at 9:00 AM
    - "* /15 * * * *": Every 15 minutes
    - "0 * /6 * * *": Every 6 hours
    - "0 0 1 * *": First day of every month at midnight
    - "0 0 * * 0": Every Sunday at midnight
    
    Tool for testing: https://crontab.guru/
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
