# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2
from .create_input_request_base import CreateInputRequestBase


class CreateCompactRequest(CreateInputRequestBase):
    """
    Requests compaction of the session history. Can be sent while the session is processing
    (queued as a follow-up) or when idle.
    """

    compact_up_to_event_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Compact all inputs up to this event ID (inclusive), e.g. "aev_abc123".
    When specified, `keep_recent_inputs` is bypassed — all inputs up to the anchor are compacted.
    If omitted, compacts all inputs except the most recent `keep_recent_inputs` inputs.
    """

    compaction_message: typing.Optional[str] = pydantic.Field(default=None)
    """
    Override the compaction prompt for this compaction.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
