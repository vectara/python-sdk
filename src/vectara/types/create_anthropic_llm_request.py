# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .anthropic_auth import AnthropicAuth
from .llm_capabilities import LlmCapabilities


class CreateAnthropicLlmRequest(UniversalBaseModel):
    """
    Request to create an Anthropic Large Language Model connection for Claude models (direct API, Bedrock, or Vertex).
    """

    name: str = pydantic.Field()
    """
    Name to reference the LLM. This will be used in other endpoints (like query) when using this LLM. If this name conflicts with a global LLM (a LLM that is preconfigured with the Vectara platform), then it will override that LLM for all usages.
    """

    description: typing.Optional[str] = pydantic.Field(default=None)
    """
    Description of the LLM.
    """

    model: str = pydantic.Field()
    """
    The Claude model name to use (e.g. claude-3-5-sonnet-20241022, claude-3-opus-20240229, etc).
    """

    uri: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional custom endpoint URI for the Anthropic API. Defaults to https://api.anthropic.com/v1/messages if not specified when using direct Anthropic API (header auth with x-api-key). Not required when using Bedrock or Vertex authentication as the endpoint is determined from region/project configuration.
    """

    auth: AnthropicAuth
    headers: typing.Optional[typing.Dict[str, str]] = pydantic.Field(default=None)
    """
    Optional additional headers to send with the request
    """

    test_model_parameters: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Any additional parameters that are required for the LLM during the test call.
    """

    capabilities: typing.Optional[LlmCapabilities] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
