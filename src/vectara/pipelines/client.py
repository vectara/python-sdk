# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.list_pipelines_response import ListPipelinesResponse
from ..types.pipeline import Pipeline
from ..types.pipeline_key import PipelineKey
from ..types.pipeline_name import PipelineName
from ..types.pipeline_run import PipelineRun
from ..types.pipeline_source import PipelineSource
from ..types.pipeline_status import PipelineStatus
from ..types.pipeline_sync_mode import PipelineSyncMode
from ..types.pipeline_transform import PipelineTransform
from ..types.pipeline_trigger import PipelineTrigger
from ..types.update_pipeline_source import UpdatePipelineSource
from .raw_client import AsyncRawPipelinesClient, RawPipelinesClient
from .types.list_pipelines_request_source_type import ListPipelinesRequestSourceType

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PipelinesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPipelinesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPipelinesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPipelinesClient
        """
        return self._raw_client

    def list(
        self,
        *,
        source_type: typing.Optional[ListPipelinesRequestSourceType] = None,
        status: typing.Optional[PipelineStatus] = None,
        enabled: typing.Optional[bool] = None,
        filter: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[Pipeline, ListPipelinesResponse]:
        """
        List all pipelines with optional filtering by source type, status, or enabled state.

        Parameters
        ----------
        source_type : typing.Optional[ListPipelinesRequestSourceType]
            Filter pipelines by source type.

        status : typing.Optional[PipelineStatus]
            Filter pipelines by status.

        enabled : typing.Optional[bool]
            Filter pipelines by enabled state.

        filter : typing.Optional[str]
            A regex filter on pipeline name and description.

        limit : typing.Optional[int]
            The maximum number of pipelines to return.

        page_key : typing.Optional[str]
            Used to retrieve the next page of pipelines after the limit has been reached.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[Pipeline, ListPipelinesResponse]
            List of pipelines.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        response = client.pipelines.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            source_type=source_type,
            status=status,
            enabled=enabled,
            filter=filter,
            limit=limit,
            page_key=page_key,
            request_options=request_options,
        )

    def create(
        self,
        *,
        name: PipelineName,
        source: PipelineSource,
        trigger: PipelineTrigger,
        transform: PipelineTransform,
        key: typing.Optional[PipelineKey] = OMIT,
        description: typing.Optional[str] = OMIT,
        sync_mode: typing.Optional[PipelineSyncMode] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Pipeline:
        """
        Create a new pipeline that continuously ingests data from a source system and sends each record to an agent for processing. A new agent session is created per source record. A pipeline is distinct from an agent schedule (which is a recurring single execution of an agent) and from a connector (which is a bidirectional chat integration like Slack). A pipeline is an automated, one-directional flow of all source data through an agent.

        Parameters
        ----------
        name : PipelineName

        source : PipelineSource

        trigger : PipelineTrigger

        transform : PipelineTransform

        key : typing.Optional[PipelineKey]
            A user-provided key for the pipeline. If omitted, one is auto-generated.

        description : typing.Optional[str]

        sync_mode : typing.Optional[PipelineSyncMode]

        enabled : typing.Optional[bool]

        metadata : typing.Optional[typing.Dict[str, str]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Pipeline
            The pipeline has been created successfully.

        Examples
        --------
        from vectara import (
            PipelineSource_S3,
            PipelineTransform_Agent,
            PipelineTrigger_Cron,
            Vectara,
        )

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pipelines.create(
            name="SharePoint Legal Docs Ingest",
            source=PipelineSource_S3(
                type="s3",
            ),
            trigger=PipelineTrigger_Cron(
                expression="0 */6 * * *",
            ),
            transform=PipelineTransform_Agent(
                agent_key="customer_support",
            ),
        )
        """
        _response = self._raw_client.create(
            name=name,
            source=source,
            trigger=trigger,
            transform=transform,
            key=key,
            description=description,
            sync_mode=sync_mode,
            enabled=enabled,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def get(self, pipeline_key: PipelineKey, *, request_options: typing.Optional[RequestOptions] = None) -> Pipeline:
        """
        Retrieve a pipeline by its key, including its current status, watermark, and configuration.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Pipeline
            The requested pipeline.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pipelines.get(
            pipeline_key="sharepoint-legal-ingest",
        )
        """
        _response = self._raw_client.get(pipeline_key, request_options=request_options)
        return _response.data

    def replace(
        self,
        pipeline_key: PipelineKey,
        *,
        name: PipelineName,
        source: PipelineSource,
        trigger: PipelineTrigger,
        transform: PipelineTransform,
        key: typing.Optional[PipelineKey] = OMIT,
        description: typing.Optional[str] = OMIT,
        sync_mode: typing.Optional[PipelineSyncMode] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Pipeline:
        """
        Replace a pipeline's configuration. The full pipeline definition must be provided.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline to replace.

        name : PipelineName

        source : PipelineSource

        trigger : PipelineTrigger

        transform : PipelineTransform

        key : typing.Optional[PipelineKey]
            A user-provided key for the pipeline. If omitted, one is auto-generated.

        description : typing.Optional[str]

        sync_mode : typing.Optional[PipelineSyncMode]

        enabled : typing.Optional[bool]

        metadata : typing.Optional[typing.Dict[str, str]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Pipeline
            The pipeline has been replaced successfully.

        Examples
        --------
        from vectara import (
            PipelineSource_S3,
            PipelineTransform_Agent,
            PipelineTrigger_Cron,
            Vectara,
        )

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pipelines.replace(
            pipeline_key="sharepoint-legal-ingest",
            name="SharePoint Legal Docs Ingest",
            source=PipelineSource_S3(
                type="s3",
            ),
            trigger=PipelineTrigger_Cron(
                expression="0 */6 * * *",
            ),
            transform=PipelineTransform_Agent(
                agent_key="customer_support",
            ),
        )
        """
        _response = self._raw_client.replace(
            pipeline_key,
            name=name,
            source=source,
            trigger=trigger,
            transform=transform,
            key=key,
            description=description,
            sync_mode=sync_mode,
            enabled=enabled,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def delete(self, pipeline_key: PipelineKey, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Permanently delete a pipeline and its run history. This cancels any in-progress runs. Agent sessions created by this pipeline are not deleted.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pipelines.delete(
            pipeline_key="sharepoint-legal-ingest",
        )
        """
        _response = self._raw_client.delete(pipeline_key, request_options=request_options)
        return _response.data

    def update(
        self,
        pipeline_key: PipelineKey,
        *,
        name: typing.Optional[PipelineName] = OMIT,
        description: typing.Optional[str] = OMIT,
        source: typing.Optional[UpdatePipelineSource] = OMIT,
        trigger: typing.Optional[PipelineTrigger] = OMIT,
        transform: typing.Optional[PipelineTransform] = OMIT,
        sync_mode: typing.Optional[PipelineSyncMode] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Optional[str]]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Pipeline:
        """
        Partially update a pipeline's configuration. Omitted fields are preserved.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline to update.

        name : typing.Optional[PipelineName]

        description : typing.Optional[str]

        source : typing.Optional[UpdatePipelineSource]

        trigger : typing.Optional[PipelineTrigger]

        transform : typing.Optional[PipelineTransform]

        sync_mode : typing.Optional[PipelineSyncMode]

        enabled : typing.Optional[bool]

        metadata : typing.Optional[typing.Dict[str, typing.Optional[str]]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Pipeline
            The pipeline has been updated successfully.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pipelines.update(
            pipeline_key="sharepoint-legal-ingest",
        )
        """
        _response = self._raw_client.update(
            pipeline_key,
            name=name,
            description=description,
            source=source,
            trigger=trigger,
            transform=transform,
            sync_mode=sync_mode,
            enabled=enabled,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def trigger(
        self, pipeline_key: PipelineKey, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PipelineRun:
        """
        Manually trigger a pipeline run outside of the normal schedule. The pipeline will fetch new data from the source and process it through the agent. Returns the created run.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline to trigger.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PipelineRun
            The pipeline run has been created.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pipelines.trigger(
            pipeline_key="sharepoint-legal-ingest",
        )
        """
        _response = self._raw_client.trigger(pipeline_key, request_options=request_options)
        return _response.data


class AsyncPipelinesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPipelinesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPipelinesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPipelinesClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        source_type: typing.Optional[ListPipelinesRequestSourceType] = None,
        status: typing.Optional[PipelineStatus] = None,
        enabled: typing.Optional[bool] = None,
        filter: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[Pipeline, ListPipelinesResponse]:
        """
        List all pipelines with optional filtering by source type, status, or enabled state.

        Parameters
        ----------
        source_type : typing.Optional[ListPipelinesRequestSourceType]
            Filter pipelines by source type.

        status : typing.Optional[PipelineStatus]
            Filter pipelines by status.

        enabled : typing.Optional[bool]
            Filter pipelines by enabled state.

        filter : typing.Optional[str]
            A regex filter on pipeline name and description.

        limit : typing.Optional[int]
            The maximum number of pipelines to return.

        page_key : typing.Optional[str]
            Used to retrieve the next page of pipelines after the limit has been reached.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[Pipeline, ListPipelinesResponse]
            List of pipelines.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            response = await client.pipelines.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            source_type=source_type,
            status=status,
            enabled=enabled,
            filter=filter,
            limit=limit,
            page_key=page_key,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        name: PipelineName,
        source: PipelineSource,
        trigger: PipelineTrigger,
        transform: PipelineTransform,
        key: typing.Optional[PipelineKey] = OMIT,
        description: typing.Optional[str] = OMIT,
        sync_mode: typing.Optional[PipelineSyncMode] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Pipeline:
        """
        Create a new pipeline that continuously ingests data from a source system and sends each record to an agent for processing. A new agent session is created per source record. A pipeline is distinct from an agent schedule (which is a recurring single execution of an agent) and from a connector (which is a bidirectional chat integration like Slack). A pipeline is an automated, one-directional flow of all source data through an agent.

        Parameters
        ----------
        name : PipelineName

        source : PipelineSource

        trigger : PipelineTrigger

        transform : PipelineTransform

        key : typing.Optional[PipelineKey]
            A user-provided key for the pipeline. If omitted, one is auto-generated.

        description : typing.Optional[str]

        sync_mode : typing.Optional[PipelineSyncMode]

        enabled : typing.Optional[bool]

        metadata : typing.Optional[typing.Dict[str, str]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Pipeline
            The pipeline has been created successfully.

        Examples
        --------
        import asyncio

        from vectara import (
            AsyncVectara,
            PipelineSource_S3,
            PipelineTransform_Agent,
            PipelineTrigger_Cron,
        )

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pipelines.create(
                name="SharePoint Legal Docs Ingest",
                source=PipelineSource_S3(
                    type="s3",
                ),
                trigger=PipelineTrigger_Cron(
                    expression="0 */6 * * *",
                ),
                transform=PipelineTransform_Agent(
                    agent_key="customer_support",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            name=name,
            source=source,
            trigger=trigger,
            transform=transform,
            key=key,
            description=description,
            sync_mode=sync_mode,
            enabled=enabled,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def get(
        self, pipeline_key: PipelineKey, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Pipeline:
        """
        Retrieve a pipeline by its key, including its current status, watermark, and configuration.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Pipeline
            The requested pipeline.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pipelines.get(
                pipeline_key="sharepoint-legal-ingest",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(pipeline_key, request_options=request_options)
        return _response.data

    async def replace(
        self,
        pipeline_key: PipelineKey,
        *,
        name: PipelineName,
        source: PipelineSource,
        trigger: PipelineTrigger,
        transform: PipelineTransform,
        key: typing.Optional[PipelineKey] = OMIT,
        description: typing.Optional[str] = OMIT,
        sync_mode: typing.Optional[PipelineSyncMode] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Pipeline:
        """
        Replace a pipeline's configuration. The full pipeline definition must be provided.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline to replace.

        name : PipelineName

        source : PipelineSource

        trigger : PipelineTrigger

        transform : PipelineTransform

        key : typing.Optional[PipelineKey]
            A user-provided key for the pipeline. If omitted, one is auto-generated.

        description : typing.Optional[str]

        sync_mode : typing.Optional[PipelineSyncMode]

        enabled : typing.Optional[bool]

        metadata : typing.Optional[typing.Dict[str, str]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Pipeline
            The pipeline has been replaced successfully.

        Examples
        --------
        import asyncio

        from vectara import (
            AsyncVectara,
            PipelineSource_S3,
            PipelineTransform_Agent,
            PipelineTrigger_Cron,
        )

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pipelines.replace(
                pipeline_key="sharepoint-legal-ingest",
                name="SharePoint Legal Docs Ingest",
                source=PipelineSource_S3(
                    type="s3",
                ),
                trigger=PipelineTrigger_Cron(
                    expression="0 */6 * * *",
                ),
                transform=PipelineTransform_Agent(
                    agent_key="customer_support",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.replace(
            pipeline_key,
            name=name,
            source=source,
            trigger=trigger,
            transform=transform,
            key=key,
            description=description,
            sync_mode=sync_mode,
            enabled=enabled,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def delete(
        self, pipeline_key: PipelineKey, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Permanently delete a pipeline and its run history. This cancels any in-progress runs. Agent sessions created by this pipeline are not deleted.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pipelines.delete(
                pipeline_key="sharepoint-legal-ingest",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(pipeline_key, request_options=request_options)
        return _response.data

    async def update(
        self,
        pipeline_key: PipelineKey,
        *,
        name: typing.Optional[PipelineName] = OMIT,
        description: typing.Optional[str] = OMIT,
        source: typing.Optional[UpdatePipelineSource] = OMIT,
        trigger: typing.Optional[PipelineTrigger] = OMIT,
        transform: typing.Optional[PipelineTransform] = OMIT,
        sync_mode: typing.Optional[PipelineSyncMode] = OMIT,
        enabled: typing.Optional[bool] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Optional[str]]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Pipeline:
        """
        Partially update a pipeline's configuration. Omitted fields are preserved.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline to update.

        name : typing.Optional[PipelineName]

        description : typing.Optional[str]

        source : typing.Optional[UpdatePipelineSource]

        trigger : typing.Optional[PipelineTrigger]

        transform : typing.Optional[PipelineTransform]

        sync_mode : typing.Optional[PipelineSyncMode]

        enabled : typing.Optional[bool]

        metadata : typing.Optional[typing.Dict[str, typing.Optional[str]]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Pipeline
            The pipeline has been updated successfully.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pipelines.update(
                pipeline_key="sharepoint-legal-ingest",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            pipeline_key,
            name=name,
            description=description,
            source=source,
            trigger=trigger,
            transform=transform,
            sync_mode=sync_mode,
            enabled=enabled,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def trigger(
        self, pipeline_key: PipelineKey, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PipelineRun:
        """
        Manually trigger a pipeline run outside of the normal schedule. The pipeline will fetch new data from the source and process it through the agent. Returns the created run.

        Parameters
        ----------
        pipeline_key : PipelineKey
            The unique key of the pipeline to trigger.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PipelineRun
            The pipeline run has been created.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pipelines.trigger(
                pipeline_key="sharepoint-legal-ingest",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.trigger(pipeline_key, request_options=request_options)
        return _response.data
