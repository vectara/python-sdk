# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.glossary import Glossary
from ..types.glossary_entries_response import GlossaryEntriesResponse
from ..types.glossary_entry import GlossaryEntry
from ..types.glossary_key import GlossaryKey
from ..types.list_glossaries_response import ListGlossariesResponse
from .raw_client import AsyncRawGlossariesClient, RawGlossariesClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class GlossariesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawGlossariesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawGlossariesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawGlossariesClient
        """
        return self._raw_client

    def list(
        self,
        *,
        filter: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[Glossary, ListGlossariesResponse]:
        """
        List all glossaries available to the authenticated user.

        Parameters
        ----------
        filter : typing.Optional[str]
            A case-insensitive substring to filter glossary names and descriptions.

        limit : typing.Optional[int]
            The maximum number of glossaries to return.

        page_key : typing.Optional[str]
            Used to retrieve the next page of glossaries.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[Glossary, ListGlossariesResponse]
            List of glossaries.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        response = client.glossaries.list(
            filter="engineering",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(filter=filter, limit=limit, page_key=page_key, request_options=request_options)

    def create(
        self,
        *,
        name: str,
        key: typing.Optional[GlossaryKey] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Glossary:
        """
        Create a new glossary. A glossary is a reusable mapping of terms to expanded forms that agents use to expand abbreviations and acronyms in user messages.

        Parameters
        ----------
        name : str
            Human-readable name for the glossary.

        key : typing.Optional[GlossaryKey]
            A unique key for the glossary. If not provided, one will be auto-generated from the name.

        description : typing.Optional[str]
            A description of what this glossary covers.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Glossary
            The glossary was created successfully.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.glossaries.create(
            name="Engineering Acronyms",
        )
        """
        _response = self._raw_client.create(
            name=name, key=key, description=description, request_options=request_options
        )
        return _response.data

    def get(self, glossary_key: GlossaryKey, *, request_options: typing.Optional[RequestOptions] = None) -> Glossary:
        """
        Get the metadata of a specific glossary.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Glossary
            The glossary metadata.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.glossaries.get(
            glossary_key="eng-acronyms",
        )
        """
        _response = self._raw_client.get(glossary_key, request_options=request_options)
        return _response.data

    def delete(self, glossary_key: GlossaryKey, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete a glossary and all its entries.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.glossaries.delete(
            glossary_key="eng-acronyms",
        )
        """
        _response = self._raw_client.delete(glossary_key, request_options=request_options)
        return _response.data

    def update(
        self,
        glossary_key: GlossaryKey,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Glossary:
        """
        Update the metadata of a glossary.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary to update.

        name : typing.Optional[str]
            Updated name for the glossary.

        description : typing.Optional[str]
            Updated description.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Glossary
            The glossary was updated successfully.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.glossaries.update(
            glossary_key="eng-acronyms",
        )
        """
        _response = self._raw_client.update(
            glossary_key, name=name, description=description, request_options=request_options
        )
        return _response.data

    def list_entries(
        self,
        glossary_key: GlossaryKey,
        *,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[GlossaryEntry, GlossaryEntriesResponse]:
        """
        List the entries in a glossary.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary.

        limit : typing.Optional[int]
            The maximum number of entries to return.

        page_key : typing.Optional[str]
            Used to retrieve the next page of entries.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[GlossaryEntry, GlossaryEntriesResponse]
            List of glossary entries.

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        response = client.glossaries.list_entries(
            glossary_key="eng-acronyms",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list_entries(
            glossary_key, limit=limit, page_key=page_key, request_options=request_options
        )

    def upsert_entries(
        self,
        glossary_key: GlossaryKey,
        *,
        entries: typing.Dict[str, str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Bulk upsert entries into a glossary. Entries with existing terms are updated and new terms are inserted. The glossary index blob is rebuilt after the upsert.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary.

        entries : typing.Dict[str, str]
            A map of terms to their expanded forms. Keys are terms (1–200 characters);
            values are expansions (1–1000 characters).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.glossaries.upsert_entries(
            glossary_key="eng-acronyms",
            entries={"k8s": "Kubernetes", "tf": "Terraform"},
        )
        """
        _response = self._raw_client.upsert_entries(glossary_key, entries=entries, request_options=request_options)
        return _response.data

    def delete_entries(
        self,
        glossary_key: GlossaryKey,
        *,
        terms: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Bulk delete entries from a glossary by term. The glossary index blob is rebuilt after deletion.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary.

        terms : typing.Sequence[str]
            The terms to remove from the glossary.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from vectara import Vectara

        client = Vectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.glossaries.delete_entries(
            glossary_key="eng-acronyms",
            terms=["terms"],
        )
        """
        _response = self._raw_client.delete_entries(glossary_key, terms=terms, request_options=request_options)
        return _response.data


class AsyncGlossariesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawGlossariesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawGlossariesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawGlossariesClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        filter: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[Glossary, ListGlossariesResponse]:
        """
        List all glossaries available to the authenticated user.

        Parameters
        ----------
        filter : typing.Optional[str]
            A case-insensitive substring to filter glossary names and descriptions.

        limit : typing.Optional[int]
            The maximum number of glossaries to return.

        page_key : typing.Optional[str]
            Used to retrieve the next page of glossaries.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[Glossary, ListGlossariesResponse]
            List of glossaries.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            response = await client.glossaries.list(
                filter="engineering",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            filter=filter, limit=limit, page_key=page_key, request_options=request_options
        )

    async def create(
        self,
        *,
        name: str,
        key: typing.Optional[GlossaryKey] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Glossary:
        """
        Create a new glossary. A glossary is a reusable mapping of terms to expanded forms that agents use to expand abbreviations and acronyms in user messages.

        Parameters
        ----------
        name : str
            Human-readable name for the glossary.

        key : typing.Optional[GlossaryKey]
            A unique key for the glossary. If not provided, one will be auto-generated from the name.

        description : typing.Optional[str]
            A description of what this glossary covers.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Glossary
            The glossary was created successfully.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.glossaries.create(
                name="Engineering Acronyms",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            name=name, key=key, description=description, request_options=request_options
        )
        return _response.data

    async def get(
        self, glossary_key: GlossaryKey, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Glossary:
        """
        Get the metadata of a specific glossary.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Glossary
            The glossary metadata.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.glossaries.get(
                glossary_key="eng-acronyms",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(glossary_key, request_options=request_options)
        return _response.data

    async def delete(
        self, glossary_key: GlossaryKey, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Delete a glossary and all its entries.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.glossaries.delete(
                glossary_key="eng-acronyms",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(glossary_key, request_options=request_options)
        return _response.data

    async def update(
        self,
        glossary_key: GlossaryKey,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Glossary:
        """
        Update the metadata of a glossary.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary to update.

        name : typing.Optional[str]
            Updated name for the glossary.

        description : typing.Optional[str]
            Updated description.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Glossary
            The glossary was updated successfully.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.glossaries.update(
                glossary_key="eng-acronyms",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            glossary_key, name=name, description=description, request_options=request_options
        )
        return _response.data

    async def list_entries(
        self,
        glossary_key: GlossaryKey,
        *,
        limit: typing.Optional[int] = None,
        page_key: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[GlossaryEntry, GlossaryEntriesResponse]:
        """
        List the entries in a glossary.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary.

        limit : typing.Optional[int]
            The maximum number of entries to return.

        page_key : typing.Optional[str]
            Used to retrieve the next page of entries.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[GlossaryEntry, GlossaryEntriesResponse]
            List of glossary entries.

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            response = await client.glossaries.list_entries(
                glossary_key="eng-acronyms",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list_entries(
            glossary_key, limit=limit, page_key=page_key, request_options=request_options
        )

    async def upsert_entries(
        self,
        glossary_key: GlossaryKey,
        *,
        entries: typing.Dict[str, str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Bulk upsert entries into a glossary. Entries with existing terms are updated and new terms are inserted. The glossary index blob is rebuilt after the upsert.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary.

        entries : typing.Dict[str, str]
            A map of terms to their expanded forms. Keys are terms (1–200 characters);
            values are expansions (1–1000 characters).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.glossaries.upsert_entries(
                glossary_key="eng-acronyms",
                entries={"k8s": "Kubernetes", "tf": "Terraform"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.upsert_entries(
            glossary_key, entries=entries, request_options=request_options
        )
        return _response.data

    async def delete_entries(
        self,
        glossary_key: GlossaryKey,
        *,
        terms: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Bulk delete entries from a glossary by term. The glossary index blob is rebuilt after deletion.

        Parameters
        ----------
        glossary_key : GlossaryKey
            The unique key of the glossary.

        terms : typing.Sequence[str]
            The terms to remove from the glossary.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from vectara import AsyncVectara

        client = AsyncVectara(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.glossaries.delete_entries(
                glossary_key="eng-acronyms",
                terms=["terms"],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_entries(glossary_key, terms=terms, request_options=request_options)
        return _response.data
