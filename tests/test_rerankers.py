# This file was auto-generated by Fern from our API Definition.

import typing

from vectara.client import AsyncVectara, Vectara

from .utilities import validate_response


async def test_list_(client: Vectara, async_client: AsyncVectara) -> None:
    expected_response: typing.Any = {
        "rerankers": [{"id": "id", "name": "name", "description": "description", "enabled": True}],
        "metadata": {"page_key": "page_key"},
    }
    expected_types: typing.Any = {
        "rerankers": ("list", {0: {"id": None, "name": None, "description": None, "enabled": None}}),
        "metadata": {"page_key": None},
    }
    response = client.rerankers.list(filter="vectara.*")
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.rerankers.list(filter="vectara.*")
    validate_response(async_response, expected_response, expected_types)
