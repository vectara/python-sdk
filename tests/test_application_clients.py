# This file was auto-generated by Fern from our API Definition.

import typing

from vectara.client import AsyncVectara, Vectara

from .utilities import validate_response


async def test_list_(client: Vectara, async_client: AsyncVectara) -> None:
    expected_response: typing.Any = {
        "app_clients": [
            {
                "id": "id",
                "name": "name",
                "description": "description",
                "client_id": "client_id",
                "client_secret": "client_secret",
                "api_roles": ["owner"],
                "api_policy": {
                    "name": "name",
                    "allowed_operations": {"allowed_operations": {"allow_any_resource": True}},
                },
            }
        ],
        "metadata": {"page_key": "page_key"},
    }
    expected_types: typing.Any = {
        "app_clients": (
            "list",
            {
                0: {
                    "id": None,
                    "name": None,
                    "description": None,
                    "client_id": None,
                    "client_secret": None,
                    "api_roles": ("list", {0: None}),
                    "api_policy": {
                        "name": None,
                        "allowed_operations": ("dict", {0: (None, {"allow_any_resource": None})}),
                    },
                }
            },
        ),
        "metadata": {"page_key": None},
    }
    response = client.application_clients.list()
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.application_clients.list()
    validate_response(async_response, expected_response, expected_types)


async def test_get(client: Vectara, async_client: AsyncVectara) -> None:
    expected_response: typing.Any = {
        "id": "id",
        "name": "name",
        "description": "description",
        "client_id": "client_id",
        "client_secret": "client_secret",
        "api_roles": ["owner"],
        "api_policy": {"name": "name", "allowed_operations": {"allowed_operations": {"allow_any_resource": True}}},
    }
    expected_types: typing.Any = {
        "id": None,
        "name": None,
        "description": None,
        "client_id": None,
        "client_secret": None,
        "api_roles": ("list", {0: None}),
        "api_policy": {"name": None, "allowed_operations": ("dict", {0: (None, {"allow_any_resource": None})})},
    }
    response = client.application_clients.get(app_client_id="app_client_id")
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.application_clients.get(app_client_id="app_client_id")
    validate_response(async_response, expected_response, expected_types)


async def test_delete(client: Vectara, async_client: AsyncVectara) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert client.application_clients.delete(app_client_id="app_client_id") is None  # type: ignore[func-returns-value]

    assert await async_client.application_clients.delete(app_client_id="app_client_id") is None  # type: ignore[func-returns-value]


async def test_update(client: Vectara, async_client: AsyncVectara) -> None:
    expected_response: typing.Any = {
        "id": "id",
        "name": "name",
        "description": "description",
        "client_id": "client_id",
        "client_secret": "client_secret",
        "api_roles": ["owner"],
        "api_policy": {"name": "name", "allowed_operations": {"allowed_operations": {"allow_any_resource": True}}},
    }
    expected_types: typing.Any = {
        "id": None,
        "name": None,
        "description": None,
        "client_id": None,
        "client_secret": None,
        "api_roles": ("list", {0: None}),
        "api_policy": {"name": None, "allowed_operations": ("dict", {0: (None, {"allow_any_resource": None})})},
    }
    response = client.application_clients.update(app_client_id="app_client_id")
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.application_clients.update(app_client_id="app_client_id")
    validate_response(async_response, expected_response, expected_types)
